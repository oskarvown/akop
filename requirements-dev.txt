-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0

# Только для генерации .xls fixtures в tests/fixtures/ (см.
# tests/fixtures/generate_regional_fixtures.py) — не нужен в production.
xlwt==1.3.0
